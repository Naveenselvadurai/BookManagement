CREATE OR ALTER PROCEDURE GET_BOOKS_SORT_PUBLISHER
AS 
BEGIN	
	SET NOCOUNT ON

		SELECT B.BOOKID,
				B.PUBLISHER,
				B.TITLE,
				B.AUTHORFIRSTNAME,
				B.AUTHORLASTNAME,
				CONCAT (AUTHORLASTNAME,' ',AUTHORFIRSTNAME) AS 'AUTHORNAME',		
				PRICE
		FROM BOOKS AS B 
		ORDER BY PUBLISHER,AUTHORNAME,TITLE

	SET NOCOUNT OFF
END